<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.care.project.repository.BoardRepository">

	<insert id="write" parameterType="com.care.project.dto.BoardDTO">
		<selectKey order="BEFORE" keyProperty="boardId" resultType="Integer">
			SELECT nvl(max(boardId),0) + 1 FROM posts
		</selectKey>
		INSERT INTO posts VALUES(#{boardId},#{title},#{content},#{writer},#{vCount},#{cDate},#{mDate},#{fName})
	</insert>
	
	<select id="boardCount" resultType="Integer"  parameterType="Map">
		SELECT count(*) FROM posts
		<choose>
			<when test="select == '작성자'">WHERE writer LIKE '%'||#{search}||'%'</when>
			<when test="select == '제목'">WHERE title LIKE '%'||#{search}||'%'</when>
		</choose>
	</select>
	
	<select id="boardList" resultType="com.care.project.dto.BoardDTO">
		SELECT B.* FROM(
			SELECT rownum rn, A.* FROM(
				SELECT * FROM posts
				<choose>
					<when test="sel == '제목'">WHERE title LIKE '%'||#{search}||'%'</when>
					<when test="sel == '작성자'">WHERE writer LIKE '%'||#{search}||'%'</when>
				</choose>
				 ORDER BY boardId DESC
			)A
		)B WHERE rn BETWEEN #{b} and #{e}
	</select>
	
	<select id="boardView" resultType="com.care.project.dto.BoardDTO">
		select * from posts WHERE boardId=#{bI}
	</select>
	
	<update id="vCountInc">
		UPDATE posts SET vCount=vCount+1 WHERE boardId=#{bI}
	</update>
	
	<delete id="boardDelete">
		DELETE FROM posts WHERE boardId=#{bI}
	</delete>
	
	<update id="boardUpdate" parameterType="com.care.project.dto.BoardDTO">
		UPDATE posts SET title=#{title}, content=#{content} WHERE boardId=#{boardId}
	</update>
	
	<insert id="commentProc" parameterType="com.care.project.dto.CommentDTO">
			<selectKey order="BEFORE" keyProperty="commentId" resultType="Integer">
				SELECT nvl(max(commentId),0) + 1 FROM comments			
			</selectKey>
			INSERT INTO comments VALUES(#{commentId}, #{content},#{cDate},#{writer},#{boardId})
	</insert>
	
	<select id="commentList" resultType="com.care.project.dto.CommentDTO">
		SELECT * FROM comments WHERE boardId=#{boardId}
	</select>
	
	<delete id="cDelete">
		DELETE FROM comments WHERE commentId=#{cId}
	</delete>

	
</mapper>